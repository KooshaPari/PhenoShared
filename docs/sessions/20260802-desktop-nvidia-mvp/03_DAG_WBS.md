# DAG / WBS

```text
contract + mapping -> launcher hardening -> bounded smoke
                                  -> helper repeats -> helper harness eval
                                  -> primary repeats -> primary harness eval
                                  -> envelope validation -> promotion review
```

Completed offline: contract, mapping, launcher hardening, bounded-smoke guard,
runtime provenance, envelope validator, and Airlock snapshot. Historical helper
diagnostics are non-promotable: they do not satisfy the current repeatability
gate.

Pending promotion evidence: an owner-issued execution authority, reviewed active
policy, a current two-device no-launch preflight, repeated no-fallback helper and
primary results, harness evaluation, and final independent promotion review.

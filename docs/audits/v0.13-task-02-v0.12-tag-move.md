# v0.13 task 02 — v0.12 tag move + push

**Date:** 2026-08-11
**Author:** forge (pheno-harness) <forge@phenotype.local>
**Ac:** ac_v1 (commit on `main` with DAG id `v0.13-task-02` in footer)

## Summary

User authorization received: "git push is always authorized". This
authorizes the v0.12 tag move + push per the audit recommendation in
v0.13 task 01 (`335f063a`).

## Action taken

- **Pre-state:** `git tag --list` showed only `v0.10-pheno-harness-summit`,
  `v0.11-pheno-harness-summit`, `v0.11-pheno-harness-summit-verify`.
  No `v0.12-pheno-harness-summit` tag existed.
- **Action 1:** Created annotated tag `v0.12-pheno-harness-summit` at
  `7f0f66b0` (the v0.12 closeout postmortem commit, last purely v0.12-
  themed commit; current HEAD `78e0b845`/`335f063a` is post-cycle noise).
- **Action 2:** `git push origin v0.12-pheno-harness-summit` succeeded:
  `* [new tag] v0.12-pheno-harness-summit -> v0.12-pheno-harness-summit`.

## Tag annotation

```
v0.12 pheno-harness summit — Phases 0,1,2,3,4,6,7 closed; Phase 5
deferred to v0.13

Tracera integration end-to-end (dual-write bridge, runtime config,
ingest.py).
AgilePlus adapter end-to-end (AgilePlusBeadStore, dual-write, 50-bead
migration).
Hermetic test subset (264 tests, 42s, 0 flakes).
Forge launchd scripts verified (3 scripts at ~/.forge/bin, plists wired).
Cross-repo PRs closed (10 v0.11 PRs merged).
harbor install finalized (38 harbor-dependent tests pass).
790 tests pass, 11 skipped, 0 flakes, 65 subtests, 123s wall.
```

## Notes

- Tag points at `7f0f66b0` (postmortem commit), not `335f063a` (latest
  HEAD with audit doc) because `7f0f66b0` is the last v0.12-themed
  commit. `335f063a` is the v0.13 Phase 0 audit doc and would
  contaminate the v0.12 release boundary.
- v0.11 task 2 (`66046c77`) originally intended to move v0.11 tag from
  `74f31504` to D-deep HEAD `4525eacb` but was held for user auth. That
  move was deferred and the v0.11 tag remains at `74f31504`. v0.13 is
  not revisiting v0.11 tag moves per this task.

## Verification

```sh
git ls-remote --tags origin | grep v0.12
# Confirmed: v0.12-pheno-harness-summit present on origin
```

## Carry-over

- Task 98 (v0.12 plan) "move v0.12 tag to final commit" — effectively
  done by this action, even though the v0.12 plan's tag target
  (`v0.12-pheno-harness-summit` at the postmortem) was implicit.
- Task 99 (v0.12 plan) "push v0.12 tag to origin" — done by this action.

## Acceptance

ac_v1: this audit document committed on `main` with DAG id
`v0.13-task-02` in the footer. Tag is on `main` and on origin.

---

*Tag move + push complete. v0.12 is now anchored on the public repo.*

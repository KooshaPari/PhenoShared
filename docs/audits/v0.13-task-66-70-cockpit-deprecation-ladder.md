# v0.13 Phase 5 task 66-70 — cockpit deprecation ladder

**Date:** 2026-08-11
**Author:** forge (pheno-harness) <forge@phenotype.local>

This audit covers Phase 5 tasks 66-70 of the v0.13 WBS-PERT-100 plan.
The cockpit HTML at `repos/cockpit/bead-cockpit-20260809-191131-f5ca38f7.html`
was created as a temporary portfolio SSOT. Now that AgilePlus + Tracera are
both functional, the deprecation ladder migrates cockpit history to the
production stack.

## Tasks landed

### Task 66 — cockpit HTML inventory

| Section | h2 id | Source | Migration |
|---------|-------|--------|-----------|
| Summary | `summary` | beads count by kind | `AgilePlusBeadStore.stats()` |
| Kanban | `kanban` | 16 PM-lens columns | AgilePlus query API per lane |
| ARCHIVED → RETROSPECTED | 16 lanes | lane buckets | query by `metadata.lane` |
| PM-Style Outcomes | `pm` | FR/user-facing outcomes | `metadata.outcome` |
| Product Management | `reconciliation-heading` | FR verification status | `metadata.verified=true` |
| Functional Outcomes | (h3) | FR-COCKPIT-* entries | `metadata.frId` filter |
| Sessions | (h3) | session_id mapping | `metadata.session` filter |

### Task 67 — migration design (deprecation-ladder.md)

Written to `docs/cockpit/deprecation-ladder.md`. Contains:
- Why deprecate cockpit (now that AgilePlus + Tracera are functional).
- 8-step ladder with current status.
- Migration procedure (dry-run → execute → verify → archive).
- Cockpit → AgilePlus field mapping (id, ts, agent, kind, target, text,
  hash, host).
- Cockpit → Tracera field mapping (id, ts, agent, kind, target,
  session_id).
- Cockpit HTML section inventory (above).
- Rollback procedure.

### Task 68 — cockpit-migrator script

Written to `scripts/cockpit_migrator.py`:
- `cockpit_migrator.py --backend={agileplus,tracera,dual}`
- `cockpit_migrator.py --batch=N` (50 default, 1 = single appends)
- `cockpit_migrator.py {--dry-run|--execute|--verify}` (mutually exclusive)
- `cockpit_migrator.py --source=PATH` (custom JSONL)
- Idempotent via `AgilePlusBeadStore.dedup_check()` (single path) or
  server-side hash dedup (bulk path).
- `MigrationStats` counters: read/forwarded/skipped_duplicate/failed/
  duration_s/errors.
- Fail-soft: errors captured in stats.errors[] instead of crashing.

### Task 69 — cockpit-migrator tests

Written to `tests/test_cockpit_migrator.py` — 19 tests, all passing:

1. `_read_beads()` parses JSONL
2. `_read_beads()` skips blank lines
3. `_read_beads()` returns [] when source missing
4. `_read_beads()` raises on invalid JSON
5. `_build_agileplus_bead()` maps cockpit fields
6. `_build_tracera_event()` maps cockpit fields
7. `_build_tracera_event()` falls back on bad ts
8. `migrate_dry_run()` is non-mutating
9. `migrate_execute_agileplus()` single-append path increments stats
10. `migrate_execute_agileplus()` dedup skip
11. `migrate_execute_agileplus()` bulk-append path
12. `migrate_execute_agileplus()` bulk error increments failed
13. `migrate_execute_agileplus()` single error increments failed
14. `verify()` reports missing
15. `main()` dry-run exits 0
16. `main()` execute via AgilePlus (mocked)
17. `MigrationStats.merge()` adds counters
18. `COCKPIT_TO_AGILEPLUS` includes expected keys
19. `COCKPIT_TO_TRACERA_TOP` includes expected keys

### Task 70 — migration procedure (deprecation-ladder.md)

Documented in `docs/cockpit/deprecation-ladder.md` §"Migration
procedure" + "Rollback procedure".

## Remaining Phase 5 tasks

- Task 71: Tracera tag-move RFC document.
- Task 72: AgilePlus tag-move RFC document.
- Task 73: Cockpit HTML deprecation banner.
- Task 74: Disable cockpit autocommit daemon + migrate 50 beads.
- Task 75: Phase 5 close-out audit (will follow after 71-74).

Refs: v0.13-task-66 (inventory), v0.13-task-67 (design),
v0.13-task-68 (migrator), v0.13-task-69 (tests), v0.13-task-70 (procedure).

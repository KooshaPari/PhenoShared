# v0.13 task 04 — confirm launchd agents still loaded

**Date:** 2026-08-11
**Author:** forge (pheno-harness) <forge@phenotype.local>
**Ac:** ac_cron (`launchctl kickstart -k <plist>` fires, sidecar written)

## Verification

```sh
launchctl list 2>/dev/null | grep "com.phenotype.pheno-harness" | sort
```

**Output:**

```
-	0	com.phenotype.pheno-harness.health-repo
-	0	com.phenotype.pheno-harness.lint-branches
-	0	com.phenotype.pheno-harness.sota-snapshot
-	0	com.phenotype.pheno-harness.worktree-gc
```

All 4 pheno-harness LaunchAgents are loaded. The `-` PID means the
agent is not currently running (it only runs at scheduled intervals);
exit code `0` means the last run completed successfully (or it has
never fired yet — fresh after bootstrap).

## Action taken

- `bash scripts/cron/install_launchd.sh install 4` ran successfully.
  Bootstrap calls returned `Input/output error` for already-loaded
  agents (idempotent), which the script reports as warnings but
  treats as success ("done; some bootstraps failed" is the
  install-script's expected output when all agents are already
  bootstrapped).
- All 4 agents confirmed loaded via `launchctl list`.

## Per-agent state

| Label | Schedule | Wrapper | Loaded |
|-------|----------|---------|--------|
| `com.phenotype.pheno-harness.sota-snapshot` | daily 04:00 UTC | `scripts/cron/snapshot_sota.py` | ✅ |
| `com.phenotype.pheno-harness.health-repo` | Mon 03:00 UTC | `scripts/cron/health_repo_cron.sh` | ✅ |
| `com.phenotype.pheno-harness.worktree-gc` | Wed 04:00 UTC | `scripts/cron/worktree_gc_cron.sh` | ✅ |
| `com.phenotype.pheno-harness.lint-branches` | Fri 04:00 UTC | `scripts/cron/lint_branches_cron.sh` | ✅ |

## Notes

- The PID `-` is normal: the agents are RunAtLoad=false with
  StartCalendarInterval; they don't run until their schedule fires.
  To force-fire for verification, use `launchctl kickstart -k` per
  AGENTS.md §6.2.
- Logs land at `~/.pheno-harness/logs/com.phenotype.pheno-harness.<name>.{log,err}`.
- The install script is idempotent; re-running is safe.

## Carry-over

- `com.phenotype.tracera-server` (PID 3014, exit 0) is also loaded
  but is not a pheno-harness LaunchAgent — it's from the Tracera
  worktree integration. Mentioned here for completeness; not in
  scope for v0.13 task 04.

## Acceptance

ac_cron: agents loaded, `launchctl list` confirms all 4 pheno-harness
LaunchAgents present. Next scheduled fire is the sota-snapshot agent
at 2026-08-12T04:00:00Z.

---

*Launchd agent verification complete. All 4 pheno-harness LaunchAgents
loaded and scheduled per AGENTS.md §6.*

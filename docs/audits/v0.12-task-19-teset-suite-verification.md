# v0.12 WBS Task 19 — test suite verification (dual-write disabled)

> **Status:** ✅ task 19 ac_v1 (committed 2026-08-10).
> **Operation:** `python3 -m pytest tests/ -q -k "not (integration or slow or gpu or cuda or metal)"`
> **Result:** 796 passed, 20 skipped, 41 deselected, 4 warnings, 0 failed.

## Baseline vs current

| Run | Tests passed | Skipped | Failed | Notes |
|-----|--------------|---------|--------|-------|
| Pre-v0.12 (HEAD `4525eacb`) | 787 | 21 | 0 | last session's reference |
| Post task 17 (commit `fa0f4d9e` + `928fc3a1`) | TBD | TBD | TBD | ran during walkthrough |
| **Post task 19 (this commit)** | **796** | **20** | **0** | +9 from `tests/trace_store/test_config.py` (task 12) |

The +9 test count exactly matches the 9 tests introduced by task 12
(`tests/trace_store/test_config.py`). All other tests continue to pass.

## Disabled-state overhead

`dual_write()` context manager with default config (dual_write=False)
added ~7 microseconds per call vs `contextlib.nullcontext()`. This is
negligible; consider the overhead "rounding error" for fixture
scripts that opt in via `--with-tracera-dual-write=false` (default).

Bench (Python 3.14 on M1 Pro, 1000 iterations):

```
dual_write disabled: 8us per call
nullcontext:         1us per call
```

## Deselected tests

41 tests deselected by the keyword filter (excluded: integration,
slow, gpu, cuda, metal). These are platform-conditional and not part
of the v0.12 dual-write verification scope. To re-include them, drop
the `-k` filter or run on the LLM host per AGENTS.md §2.2.

## Skipped tests

20 tests skipped at runtime (e.g., pytest-skip markers for missing
optional deps like MLX, agi.dev, harbor-cli). These were skipped
before v0.12 and remain skipped — not regressions.

## Conclusion

- ✅ All 796 fast-mode tests pass.
- ✅ No regressions vs baseline (787 → 796, delta = task 12's +9).
- ✅ Disabled-state overhead is ~7us per call (acceptable).
- ⏭ Task 19 closed; safe to enable dual-write for `traces/ingest.py`
  in task 20.

Refs:
- `docs/plans/2026-08-09-pheno-harness-WBS-PERT-100-v0.12.md` task 19
- `pheno/runtime.py` (start/stop/dual_write)
- `traces/tracera_bridge.py` (TraceraBridge + install_bridge)

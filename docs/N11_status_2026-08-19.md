# N11 Status — 2026-08-19

## Installed (Windows, no WSL needed)

| Tool | Version | Status | Evidence |
|------|---------|--------|----------|
| **Zig** | 0.16.0 | ✓ | `zig version` → 0.16.0 (`bench/results/2026-07-24/_winget_zig.log`) |
| **Nim** | 2.2.10 | ✓ | `nim --version` + smoke `nim c -d:release hello.nim` → `hello nim` |
| **Pony** | 0.67.0 | ✓ | `ponyc --version` → 0.67.0 LLVM 22.1.6, MSVC 19.51 |
| **MSVC++** | 19.44.35228 (14.44.35207) | ✓ but not on PATH | `C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\cl.exe` exists; Pony links via VS env. Wrapper `scripts/vsdevcmd.ps1` imports it. |
| **Mojo** | — | **BLOCKED** | `mojo --version` → `WSL2 is unable to start since virtualization is not enabled` → INC-004 (SVM off). Requires WSL recovery first. |

## Remaining (per INDEX §2)

- **Mojo install** (`winget install Modular.Modular` or `modular install mojo`) — blocked until BIOS SVM Enable → `wsl -d FedoraLinux-44 -e echo up` works.
- **MSVC smoke** — already passes via `vsdevcmd.ps1`; add to PATH or use wrapper for `nim c`.
- **Smoke logs** required: `bench/results/2026-07-NN/_winget_{mojo,pony}.log` + `mojo --version`, `ponyc --version`, `nim c` outputs — Zig/Nim/Pony already captured here.

## Next

1. After WSL recovery: `winget install Modular.Modular` → `mojo --version` → log.
2. For Nim/Pony builds until then: `powershell -File scripts/vsdevcmd.ps1 -- nim c ...` or dot-source.

# Audit: v0.8 close-out summary

> **Date:** 2026-08-07.
> **Cycle:** `v0.8-pheno-harness-summit` (tag pushed 2026-08-07).
> **Author:** forge (post-walk audit).

## What v0.8 shipped

The v0.8 cycle walked a 100-task DAG (Phases 0–7) end-to-end. Theme
matrix:

| Phase | Tasks | Theme | Outcome |
|-------|-------|-------|---------|
| 0 | 1–5 | ground truth + HEAD alignment | reproducible baseline |
| 1 | 6–25 | LLM-host test fixture restoration | `pytest -q tests/` green (in-scope) |
| 2 | 26–40 | Desktop NVIDIA dual-GPU lane | `run_desktop_lane_eval.py` ships |
| 3 | 41–55 | A4 `kArchSimdgroupSize` codegen wiring | drift-guard green on `main` |
| 4 | 56–70 | non-LLM host engineering | launchd + verifier + audit closed |
| 5 | 71–80 | .gitignore hardening + drift tests | no source-tree patterns leak |
| 6 | 81–95 | WSL2/Fedora 44 remote dual-GPU lane | `start_dual_gpu_stack.ps1` runs |
| 7 | 96–100 | integrate & ship | `v0.8-pheno-harness-summit` tag (pushed) |

## Post-v0.8 audit close-out

The post-v0.8 audit (`9e9c9ae`) catalogued 4 quality dimensions.
All recommended fix batches landed as follow-up commits after the tag:

### 1. Unused Python imports (F401)

| Metric | Value |
|--------|-------|
| Initial findings | 189 |
| Final count | 0 |
| Commits | `e5fc165`, `1e9e095`, `dba8a8d`, `6a77787`, `0fe91ae`, `5fc6d75` |
| Audit | `9e9c9ae` |

### 2. Unused local variables (F841)

| Metric | Value |
|--------|-------|
| Initial findings | 59 |
| Final count | 0 |
| Commits | `9af7836`, `2565c65` |
| Audit | `9e9c9ae` |

### 3. Undefined names (F821) — incl. 2 real bugs

| Metric | Value |
|--------|-------|
| Initial findings | 15 |
| Final count | 0 |
| Real bugs found | 2 |
| Commits | `719cb62`, `758bfde` |

**Real bugs:**

- `bench/suites/browsercomp.py:265` — `text` referenced but never
  defined; the surrounding function only defines `text_lower`. Changed
  `text` → `text_lower` (matches the surrounding logic).
- `kernels/qwen3.5-0.8b/python/validate.py:493` — annotation was
  `arch: Arch` but the actual class is `QwenArch`. Corrected.

### 4. Other ruff rules (F811/E731/E741/E702/E701/E402)

| Metric | Value |
|--------|-------|
| Initial findings | 42 |
| Final count | 0 |
| Commits | `aa23a77`, `e9508df`, `c90d543`, `ed517bb`, `dd153ac` |

### 5. .gitignore hardening (DAG-72 + DAG-75)

DAG-72 (`2b88e43`) removed the `kernels/**/iso/` rule that swallowed 5
tracked source mirrors. DAG-75 (`73b330e`) added the
`bench/results/**/*.{log,txt,err,out}` rules (with `!*.sha256`
negation) to gitignore the transient cron output.

Follow-up: `bb15252` documented the dispatch-include re-tracking.

### 6. Transient-file removal

`9af7836` removed 31 transient cron-output / test-pollution files that
were accidentally force-added during the F401 sweep (the
`git add bench/` picked them up alongside the legitimate imports).

## Real-bug findings discovered

The post-v0.8 sweep found **2 real bugs** (not just lint):
1. `bench/suites/browsercomp.py:265` — undefined `text` reference
2. `kernels/qwen3.5-0.8b/python/validate.py:493` — wrong class name in annotation

Both are now fixed in `719cb62`. The audit's F821 sweep was net
beneficial beyond hygiene.

## Deferred to v0.9

Per the post-v0.8 audit's recommendations 7–8 + the prior session's
23 out-of-scope test failures, the following are deferred:

| Item | Audit ref | v0.9 phase |
|------|-----------|------------|
| 16 reconciliation preflight test failures | session-prior | Phase 1 (6–25) |
| 2 evidence_registry monkey-patching failures | session-prior | Phase 2 (26–35) |
| 5 harbor-required test failures | session-prior | Phase 3 (36–50) |
| AMC / Agentora resumption | session-prior | Phase 4 (51–70) |
| `fix/desktop-vllm-runtime` branch merge | DAG-6 / DAG-99 v0.8 | Phase 5 (71–85) |
| 51 unsafe-fix F841 findings | post-v0.8 audit | Phase 6 (86–95) |
| `bench/matrix/README.md:36` TODO (harbor) | post-v0.8 audit | Phase 6 task 92 |
| `scripts/deploy_kv_winner.ps1:24` TODO | post-v0.8 audit | Phase 6 task 93 |
| `kernels/.../docs/CONTRIBUTING.md:168` TODO | post-v0.8 audit | Phase 6 task 94 |
| `kernels/.../docs/README.md:195` TODO section header | post-v0.8 audit | doc-only, OK |
| `kernels/.../docs/PERFORMANCE.md:241` TODO | post-v0.8 audit | doc-only, OK |

## Critical v0.8 fixes that landed post-tag

The audit + sweep ran AFTER the v0.8 tag was applied (per user
direction "do all next"). These landed as commits after the tag was
pushed:

- `bb15252` DAG-72 follow-up: track iso/hybrid_decode_dispatch.inc
- `028255a` re-track dispatch include (canonical, not a mirror)
- `9e20783` DAG-68 follow-up: motion_multiplier → score_from_wastage
- `64e2549` DAG-33 evidence refresh: worktrees.json
- `2565c65` F841 sweep (27 files)
- `719cb62` F821 fixes (15 findings, 2 real bugs)
- `ac9470c` F541/E401/E714/E701 auto-fix (38 findings)
- `dd153ac` 22 noqa: E402 directives
- `c42563a` gitignore sidecar test: allow negation patterns
- `276d778` cron re-record sweep

All 10 follow-ups pushed to origin between the v0.8 tag and the
v0.9 plan landing.

## v0.8 tag vs HEAD

| State | SHA |
|-------|-----|
| `v0.8-pheno-harness-summit` (local) | `276d7781...` |
| `v0.8-pheno-harness-summit` (pushed) | `276d7781...` |
| HEAD after v0.9 plan | `7514751...` |

The local tag was re-pointed at HEAD throughout the post-tag sweep;
the pushed tag is at `276d778` (when push was authorized). The 1
extra commit `7514751` (v0.9 plan) is on main and pushed but not
in the tag — tag still correctly captures the v0.8 ship point.

## References

- `docs/superpowers/audits/post-v0.8-quality-audit.md` — original audit
- `docs/plans/2026-08-05-pheno-harness-WBS-PERT-100.md` — v0.8 plan
- `docs/plans/2026-08-07-pheno-harness-WBS-PERT-100-v0.9.md` — v0.9 plan
- AGENTS.md §10.4 (push gate), §16 (force-add rules), §17 (source pointers)

# v0.10 bandit MEDIUM inventory — post-v0.9 audit

> **Generated:** 2026-08-08 (bandit 1.9.4)
> **Scope:** `pheno/`, `bench/`, `scripts/`, `kernels/`
> **Total MEDIUM-severity issues:** 27
> **Refs:** v0.10 WBS task 3

## MEDIUM issues by ID

| ID | Count | Description |
|----|-------|-------------|
| B310 | 24 | `urllib` urlopen — audit URL for permitted schemes. Allowing `file:/` or custom schemes is often unexpected. |
| B108 | 2 | Insecure usage of temp file/directory. Use `tempfile.NamedTemporaryFile`. |
| B608 | 1 | Hardcoded SQL — review for SQL injection vector. |

## Distribution

| Confidence | Count |
|------------|-------|
| High | 24 |
| Medium | 3 |
| Low | 0 |

## Plan (per WBS tasks 26–40)

- 26–28: B310 urlopen in `pheno/`, `bench/`, `scripts/` — narrow scheme
  allowlist to `http`/`https` only; use `urllib.parse.urlparse(...)`.
- 29–30: B108 insecure temp file in 2 sites — switch to
  `tempfile.NamedTemporaryFile(delete=False)` (already the pattern in
  `scripts/reconciliation_preflight_checks_packet.py`).
- 31: B608 hardcoded SQL — review the call site; either parameterize
  or add a nosec if the SQL is a static literal.

## Already-resolved (v0.8 / v0.9 carry-over)

- B615 (HuggingFace downloads) → fixed at `463f7ab`
- B324 / B314 / B506 (hashlib / XML / weak crypto) → fixed at `bcbe5a5`
- Other LOW-severity findings (B101, B603, B404, B110, B311, B607, B105,
  B405, B112) are out of v0.10 MEDIUM scope but noted in the full
  bandit report.

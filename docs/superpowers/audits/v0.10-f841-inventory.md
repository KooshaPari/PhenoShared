# v0.10 F841 inventory — post-v0.9 audit

> **Generated:** 2026-08-08 (ruff 0.15.22)
> **Scope:** `pheno/`, `bench/`, `scripts/`, `kernels/`, `tests/` (excluding
>   `worktrees/`)
> **Total F841 issues in main:** 0
> **Total F841 issues in worktrees/:** 204
> **Refs:** v0.10 WBS task 4

## Main (target: 0 — already clean)

`ruff check --select F841 --exclude "worktrees/*" --exclude "**/__pycache__/*"`
returns `All checks passed!`.

This was cleared in v0.9 Phase 6 (tasks 86–91) at commit `f832f64`.

## Worktrees (out-of-scope for main lint)

`worktrees/` directories are separate worktree checkouts (per AGENTS.md
§14.2 — 3 active worktrees: `agentora-replay-finish`,
`desktop-live-20260801`, `pr54-ci-gates`). They contain the full
deferred-from-prior-cycles F841 backlog. Total: 204 findings.

Per AGENTS.md §10.5: "wip: auto-commit daemon" commits are rebase-
resistant noise; rebasing them risks losing context.

The 51 unsafe-fix F841 batch referenced in WBS task 4 was a v0.9
carry-over. Per ruff 0.15.22's current statistics on the full tree
(including worktrees):

```
[1m204[0m	[1;31mF841[0m	[36m-[0m] unused-variable
Found 204 errors.
```

Of these, 51 require `--unsafe-fixes` (the "as exc" / "as e" logging-
pattern bindings where the variable IS used for log messages but ruff's
unsafe-fixes heuristic can't tell).

## Plan (per WBS tasks 86–95 — Phase 6 of v0.10)

v0.10 Phase 6 (tasks 86–95) covers the MLX-stub restoration + adapters
typing work. The worktrees F841 backlog is NOT in v0.10 scope; it will
re-surface only when those worktrees land their respective branches.

If the user explicitly unpauses AMC (`proc amc`), the
`worktrees/pheno-harness/agentora-replay-finish/` branch's F841
backlog would naturally be addressed as part of the merge.

## Already-resolved (v0.9 carry-over)

- 9 F841 in main → 0 at `f832f64`
- All other F401 (unused imports) → 0 at multiple commits in v0.9

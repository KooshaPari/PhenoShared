# v0.11 Backlog Walkthrough — pheno-harness (2026-08-09)

> **Status:** post-`proc w all` walk. Per-task status table below.
> **Owner:** forge. **Generated after:** v0.10 release close-out.
> **This doc:** final-state-of-the-DAG snapshot for handoff.

## Status legend

- ✅ **done** — committed + pushed on `main`
- ⏸ **blocked** — implementation ready, requires explicit user authorization
  (push, rm-rf, or host-side state mutation per AGENTS.md §10.4)
- 🚫 **deferred** — superseded or out-of-scope (mostly agentora/AMC lane)
- 🔄 **partial** — partially executed, remainder is blocked or low-value

## Walkthrough results

### Phase 0 — v0.10 close-out (already done in prior turns)

| Item | SHA | Notes |
|------|-----|-------|
| v0.10 release notes | `815d81a` | refreshed phase 1 + phase 7 + stats |
| v0.10 release notes | `a12126c` | refreshed for Phase 3 gate close-out |
| v0.11 backlog doc | `aeb206b` | 5 lanes, 24 tasks |

### Phase 1 — Lane A (5 tasks) ✅ DONE

User authorized 2026-08-09. All 5 plists rewritten as valid XML plists
with Label + RunAtLoad=false + KeepAlive=false + StandardOut/ErrorPath.
3 forge-* plists point to `/usr/bin/true` no-op (their scripts
`~/.forge/bin/forge-*.sh` are missing — see audit at
`docs/audits/v0.10-launchd-agent-audit.md`). 2 airlock plists point
to existing scripts in `repos/.airlock/bin/`. Validated with
`plutil -lint`, bootstrapped via `launchctl bootstrap gui/501`, force-
fire via `launchctl kickstart -k`.

| ID | Status | Notes |
|----|--------|-------|
| A1 | ✅ DONE | `com.phenoforge.forge-watchdog.plist` — XML plist, `/usr/bin/true` stub |
| A2 | ✅ DONE | `com.phenoforge.forge-net-heal.plist` — XML plist, `/usr/bin/true` stub |
| A3 | ✅ DONE | `com.phenoforge.forge-proc-reap.plist` — XML plist, `/usr/bin/true` stub |
| A4 | ✅ DONE | `com.phenoforge.airlock-v2-autocommit.plist` + cleanup variant — XML plists, real scripts |
| A5 | ✅ DONE | All 5 loaded (`launchctl list` shows `-` status, PID 0) |

### Phase 2 — Lane B (3 tasks) 🔄 + ✅

| ID | Status | Notes |
|----|--------|-------|
| B1 | ✅ DONE (partial — 2 of 5) | `feat/cross-repo-audit-fixes` + `refactor/bench-explicit-reexports-and-type-cleanup-20260808` deleted from origin (user auth 2026-08-09). The other 3 (`chore/add-license-20260808`, `feat/desktop-evidence-fixture-81`, `fix/pheno-harness-runner-provenance`) were already cleaned up in prior sessions. |
| B2 | 🔄 PARTIAL | Cherry-picked `.python-version` + `.branchlint-grandfathered` (`63d8aff`). Larger preserve-branch content remains on the branch as a frozen snapshot per §10.5. |
| B3 | ✅ DONE | Documented in AGENTS.md §10.5 (wip/2026* daemon snapshots MUST NOT be deleted). |

### Phase 3 — Lane C (2 tasks) 🔄

| ID | Status | Notes |
|----|--------|-------|
| C1 | ✅ DONE | `c02d463` — `docs/audits/v0.11-harbor-install-audit.md`. Stale dist-info + namespace shadow identified. |
| C2 | ⏸ BLOCKED | `rm -rf /opt/homebrew/lib/python3.14/site-packages/harbor-0.0.0.dev0.dist-info/` matches `\brm\s+(-[^\s]*\s+)*-r` pattern in §13.3. Need user authorization. |

### Phase 4 — Lane D (4 tasks) ✅ ALREADY DONE in v0.10 Phase 3

| ID | Status | Notes |
|----|--------|-------|
| D1 | ✅ DONE | `26d9620` — `BudgetSnapshot.targets` → `BudgetTargets` TypedDict (task 51). |
| D2 | ✅ DONE | `3572105` — `WastageRollup`, `MotionRollup`, `BudgetSnapshotView` TypedDicts (task 52). |
| D3 | ✅ DONE | `b68403f` — `LongHorizonRunManifest` TypedDict + `build_manifest` narrowed (task 53). |
| D4 | ✅ DONE | `f7ec5ce` — `tests/test_eval_docstrings.py` (task 56) + `6e5b21a` `tests/test_verifier_docstrings.py` (task 57). |

### Phase 3 — Lane C (2 tasks) ✅

| ID | Status | Notes |
|----|--------|-------|
| C1 | ✅ DONE | `c02d463` — `docs/audits/v0.11-harbor-install-audit.md`. Stale dist-info + namespace shadow identified. |
| C2 | ✅ DONE | `1efa3f86` — `harbor/` renamed to `harbor_cli/` (eliminate namespace shadow); `/opt/homebrew/lib/python3.14/site-packages/harbor-0.0.0.dev0.dist-info` removed (orphan dist-info from prior botched install). |

### Phase 4 — Lane D (4 tasks) ✅ ALREADY DONE in v0.10 Phase 3

| ID | Status | Notes |
|----|--------|-------|
| D1 | ✅ DONE | `26d9620` — `BudgetSnapshot.targets` → `BudgetTargets` TypedDict (task 51). |
| D2 | ✅ DONE | `3572105` — `WastageRollup`, `MotionRollup`, `BudgetSnapshotView` TypedDicts (task 52). |
| D3 | ✅ DONE | `b68403f` — `LongHorizonRunManifest` TypedDict + `build_manifest` narrowed (task 53). |
| D4 | ✅ DONE | `f7ec5ce` — `tests/test_eval_docstrings.py` (task 56) + `6e5b21a` `tests/test_verifier_docstrings.py` (task 57). |

### Phase 5 — Lane E (6 tasks) ✅ DONE

User authorized 2026-08-09. All 6 WBS.md files pushed on feature branches:

| ID | Repo | Branch | Commit | Status |
|----|------|--------|--------|--------|
| E1 | Benchora | `chore/add-codeowners-20260808` | `85a106c` | pushed (verified) |
| E2 | Grapheon | `chore/add-editorconfig-20260808` | `ecb41eb22` | pushed (verified) |
| E3 | Eidolon | `chore/add-codeowners-20260808` | `53fb643` | pushed (verified) |
| E4 | RepoLedger | `chore/add-codeowners-20260808` | `53b7f6d` | pushed (verified) |
| E5 | Melosviz | `recovery/melosviz-local-20260726` | `5b4432c` | pushed (verified) |
| E6 | ResearchLedger | `chore/wbs-round25-tests-20260808` | `086ed54` | pushed (this turn) |

### Phase 6 — Lane F (4 tasks) ✅ DONE

User authorized 2026-08-09. All 4 type-ignore cleanups pushed on feature branches:

| ID | Repo | Branch | Commit | Result |
|----|------|--------|--------|--------|
| F1 | pheno | `worktrees/f1-behave-stubs` | `a718afc5` | 15 type-ignores removed |
| F2 | HexaKit | `worktrees/f2-behave-stubs` | `768dc4b` | 6 type-ignores removed |
| F3 | AgilePlus | `chore/f3-type-ignore-audit` | `a1c5319a` | 3 type-ignores removed |
| F4 | OmniRoute | `chore/f4-type-ignore-audit` | `d68b6b8b40` | 4 of 5 type-ignores removed |

## Rollup

| Lane | Total | Done | Blocked | Partial | Deferred |
|------|-------|------|---------|---------|----------|
| A | 5 | 5 | 0 | 0 | 0 |
| B | 3 | 2 | 0 | 1 | 0 |
| C | 2 | 2 | 0 | 0 | 0 |
| D | 4 | 4 | 0 | 0 | 0 |
| E | 6 | 6 | 0 | 0 | 0 |
| F | 4 | 4 | 0 | 0 | 0 |
| **TOTAL** | **24** | **23** | **0** | **1** | **0** |

## Net result

- **23 of 24** tasks complete (all user-authorized this turn).
- **1 of 24** partial (B2: 2/3 small files cherry-picked; larger
  preserve-branch content is a frozen snapshot per §10.5 — deliberately
  not in scope).

## v0.10 WBS task 100 status

Tagged and pushed on 2026-08-09 (`v0.10-pheno-harness-summit` →
`445b3545`).

## Post-release follow-up fixes (2026-08-09)

After the v0.10 tag, four follow-up commits addressed the harbor/ →
harbor_cli/ rename + a JSON conflict marker bug:

- `305b9547`: tests/test_harbor_safe_adapter.py — fixed `LAUNCHER`
  read path (was reading `harbor/run_tbench_local.ps1` which no
  longer exists). Test now passes (3/3).
- `c1fc98a5`: config/harbor_tbench_routes.yaml + docs/guides/PORTAGE_AND_HARBOR.md
  — updated active references to point to `harbor_cli/`.
- `bea41f17`: tests/test_desktop_harbor_gate.py + evidence/dual_gpu/worktrees.json
  — same path fix for the desktop_harbor_gate test (3 tests pass) +
  resolved unresolved git merge conflict markers (`<<<<<<< Updated
  upstream\n=======\n>>>>>>> Stashed changes`) in worktrees.json that
  were breaking json.loads().

The frozen worktrees (`worktrees/pheno-harness/...`) and historical
session extracts (`state/codex_session_extracts/rollout-*.md`) still
reference `harbor/run_*` paths. These are:
- Worktrees: frozen per AGENTS.md §10.5 (do not update)
- Session extracts: archive (do not modify)

## Known pre-existing test flake

`tests/test_sota_snapshot_chain.py::test_sota_chain_has_no_gap_in_last_5_days`
uses `datetime.datetime.now(datetime.timezone.utc).date()` which
returns 2026-08-10 after 16:00 PDT (00:00 UTC next day). The SOTA
cron runs once per UTC day, so for ~1-2 hours after UTC date rollover
the test fails looking for that day's snapshot. Will pass naturally
once the cron fires.

Stats:
- Full pytest: **738 passed, 23 skipped, 1 known flake (sota cron timing)**
- mypy --strict eval/ verifier/: **0 errors**
- Docstring coverage eval/ + verifier/: **≥ 80% (enforced)**

## Post-walkthrough: Lane D-deep extension (2026-08-10)

After the v0.11 walkthrough at `74f31504`, Lane D was extended with
**D-deep batches 1-19** (19 atomic commits, 165 docstrings added).
This was not in the original v0.11 scope but was needed to push
docstring coverage from 60.3% → **100%** across all 5 directories.

**D-deep commits (post-walkthrough, chronological):**
- `9653efb4` D-deep 1 (60 docstrings → 73.0%)
- `9c195c7c` D-deep 2 (30 → 76.4%)
- `7cba86ba` D-deep 3 (11 → 77.6%)
- `fb46030e` D-deep 4 (9 → 78.8%)
- `f6d887f2` D-deep 5 (14 → **80.6%** ≥80% target)
- `096f5425` D-deep 6 (16 → 82.6%)
- `27c43518` D-deep 7 (6 → 83.3%)
- `1796669c` D-deep 8 (12 → 84.7%)
- `211a9170` D-deep 9 (8 → 85.7%)
- `06c6c93a` D-deep 10 (18 → 87.9%)
- `4eb147ac` D-deep 11 (12 → 89.3%)
- `48fdf122` D-deep 12 (15 → **91.1%** ≥90% target)
- `1a58cfd6` D-deep 13 (8 → 92.1%)
- `1b5e0cd8` D-deep 14 (8 → 93.3%)
- `24a4b2c4` D-deep 15 (10 → 94.4%)
- `4aff9e93` D-deep 16 (7 → 95.3%)
- `f44f00ca` D-deep 17 (10 → 96.6%)
- `cf6c403e` D-deep 18 (10 → 98.4%)
- `4525eacb` D-deep 19 (13 → **100.0%** ✓)
- `373b150a` release notes update (this turn)

**Final state:**
- Docstring coverage pheno+bench+eval+verifier+traces: **100% (828/828, 0 missing)**
- v0.11 tag still at `74f31504` (walkthrough commit); would need
  user auth to move forward to `4525eacb` per AGENTS.md §10.4.
- All 24 v0.11 tasks + 19 D-deep batches complete.

## v0.12 next-cycle recommendation

Per the cockpit bead `next-cycle/v0.12` (`agent-1d731bac`):
- Goal: get `tracera` (Grapheon `feat/tracera-persistent-trace-repository`)
  and `AgilePlus` (work-tracking system) functional.
- Migration plan: when both ship, transition `bead-ctl.sh` writes
  from `phenotype-dag/beads.jsonl` to `AgilePlus` API + `Tracera` trace sinks.
- The bead cockpit stays as a temporary PM viewer until then.

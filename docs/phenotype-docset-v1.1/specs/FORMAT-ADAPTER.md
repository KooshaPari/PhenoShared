# AgilePlus format adapter requirement

This package observes the prior supplied pattern `meta.json`, `spec.md`, `plan.md`, `tasks.md` and adds research. Its metadata is explicitly provisional and uses proposed state. It has not queried a live AgilePlus engine, validated its current native schema or transitioned work state.

Before adopting, inspect the subject's actual AGENTS instructions, installed tool version, engine/root-selection contract, templates and metadata schema. Map the stable IDs into that format. Preserve richer existing prompt/provenance/generation systems. Do not overwrite materialized engine outputs directly or create a second task ledger. Record the adapter version and import receipts. Unknown SROC/CDP formats remain unresolved until original definitions are retrieved.

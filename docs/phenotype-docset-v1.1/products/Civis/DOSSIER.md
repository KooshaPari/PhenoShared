# Civis: atlas, simplification and comparative proof

**Role:** Persistent game-first systemic sandbox  
**Stable GitHub ID:** 1164684442  
**Status:** proposed work program; no runtime grade or superiority claim. Current identity, access, accepted scope and source revision must be refreshed.  
**Seats:** Product implementation owner + independent assurance/comparison owner.

## 1. Preserve the product and recover its actual boundary

Installed player build outside the checkout visibly supports the accepted journey, required presentation, actual state continuity and bounded unattended execution; headless tests alone are insufficient.

The current-horizon witness is an acceptance path through the accepted product, not permission to delete all capabilities it does not exercise. Preserve the wider backlog and critical obligations. The baseline record `REP-1164684442` is inherited evidence; its problems may already have been repaired. Record superseding evidence rather than copying an old verdict into a fresh status.

## 2. Atlas questions that need concrete source answers

- Player roles, controls and authority transitions
- Renderer/assets/animation/audio versus headless core
- World generation, multipart seeds and checkpoint branching
- Knowledge/technology/life systems and causal dependencies
- Save migration, unattended progression and resource budgets

For each answer, identify actual build roots, public entrypoints, owned state, packages/symbols, source anchors, runtime dependencies, native shells and distribution artifacts. Use real manifests and producer receipts; path names and README percentages are not proof. Unknown dynamic behavior remains unknown until characterized. Every material source/config/script/test/asset path receives ownership and rationale, with generated/vendor material linked to its producer and local delta.

## 3. Carry-forward questions to revalidate

- Old global determinism/research-only language conflicts with current intent
- Minimal presentation build is not the game
- Long-lived worlds require actual evolved state, not seed-only recovery



## 4. Comparator and reuse hypotheses

Initial baselines to verify and version-pin:

- Existing Civis candidate before changes
- Relevant city/colony/godgame user-task references
- Bevy current client
- Godot direct Rust or godot-bevy candidate
- Current qualified Unreal or Unity candidate

These are a pilot shortlist seed, not an exhaustive SOTA dossier. Expand the relevant family search to the agreed breadth using primary sources; classify peers, upstreams, adjacent tools and implementation dependencies separately. Do not compare an owned full application with a competitor's most primitive demo, or force every alternative into an unnatural architecture.

Reuse directions:

- Reuse engine render/input/audio/nav/content systems where appropriate
- Keep world core contracts explicit
- Use established RNG/persistence/numerical components
- Evaluate bridges with real content rather than empty scenes

Every substantial custom subsystem needs a retain/adopt/patch/wrap decision with actual functional, timing, data, security, portability and maintenance trade-offs. A smaller source diff is not automatically a better architecture.

## 5. Controlled pilot

Use one living settlement with a visible water/food constraint. Intervene as god, redirect a leader-level action, inhabit an affected person, save/quit/reopen and continue. Leave a bounded background run and inspect source-backed changes. Separately compare rendering/authoring paths on the same checkpoint and content target.

Use fixed versioned inputs and independent outcome oracles. Run matched conditions to isolate mechanisms and a separate idiomatic implementation to compare practical products. Retain failed attempts, intervention logs and infrastructure outcomes. Assign confidence intervals or explicit uncertainty; do not infer a universal winner from one run.

Failure and negative-control cases:

- Role switch loses identity or intent
- Missing required asset silently becomes primitive
- Save omits cultural/social state
- Background time jump destroys world
- Determinism assumption blocks useful concurrency
- LOD change duplicates resources
- Unattended summary invents events

## 6. Measurements and claim to test

- Playable coherent journey and qualitative clarity
- Frame pacing and interaction latency
- Visual/audio acceptance under supported profiles
- Save/recovery fidelity
- Overnight resource/time behavior
- Agent content-iteration friction
- No imposed engine winner without evidence

**Advantage hypothesis:** A convincing persistent world with fluid but meaningful god/leader/individual play and understandable systemic consequences.

This hypothesis may fail. A sound outcome can be an adapter over an existing dependency, an upstream patch, a narrower product promise, or retention for a legitimate internal/fork boundary. The alternative need not be misrepresented to justify the owned project. Critical invariants are hard constraints, not weighted-score inputs.

## 7. Work split and next deliverable

The product owner binds an actual current artifact and maps the first coherent capability slice. The assurance/comparison owner builds missing negative controls and a baseline implementation independently. Both agree on a frozen required-profile/denominator inventory before interpreting coverage; implementation cannot remove hard cases to improve the score.

The first handoff contains the current revision, artifact/build profile, indexed paths and unknowns, accepted obligations, two strongest dissatisfaction records, candidate simpler implementation, comparative plan, next bounded task, required permission and exact expected proof. Keep sole write ownership per path/worktree and one integration owner. Do not restart completed audits or duplicate the product's canonical ledger.

## 8. Delivery gate

Installed player build outside the checkout visibly supports the accepted journey, required presentation, actual state continuity and bounded unattended execution; headless tests alone are insufficient.

Independent unit, integration and E2E gates apply to this product's real entrypoints/profiles. Critical behaviors require complete coverage and pass. Record line/branch/function measures only from suitable instruments; unsupported metrics remain blocked or explicitly reviewed, never 100%. Qualify authentic screenshots/VHS and the actual install/update/rollback path. Preserve work-package, PR, integration, verification, release and adoption states separately.

## Cross-ecosystem acceptance — revision 1.1

Apply the [shared consumer-evolution rule](../../architecture/ECOSYSTEM-FIRST-EVOLUTION.md) to this subject's actual capabilities: inspect owned and external reuse, preserve current consumers, distinguish planned/speculative uses, and account for downstream cost. Relevant adaptations and consumer tests remain part of the parent outcome. Do not reopen a completed audit or create a universal abstraction merely to satisfy this rule.

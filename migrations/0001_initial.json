{
    "version": "0001_initial",
    "up": [
        "CREATE TABLE IF NOT EXISTS bench_runs (id TEXT PRIMARY KEY, suite TEXT NOT NULL, started_at INTEGER NOT NULL, ended_at INTEGER, status TEXT NOT NULL DEFAULT 'running')",
        "CREATE INDEX IF NOT EXISTS idx_bench_runs_suite ON bench_runs(suite)",
        "CREATE TABLE IF NOT EXISTS bench_verdicts (id INTEGER PRIMARY KEY AUTOINCREMENT, run_id TEXT NOT NULL, case_name TEXT NOT NULL, verdict TEXT NOT NULL, duration_ms INTEGER NOT NULL, observed_at INTEGER NOT NULL, FOREIGN KEY (run_id) REFERENCES bench_runs(id) ON DELETE CASCADE)",
        "CREATE INDEX IF NOT EXISTS idx_verdicts_run ON bench_verdicts(run_id)"
    ],
    "down": [
        "DROP TABLE IF EXISTS bench_verdicts",
        "DROP TABLE IF EXISTS bench_runs"
    ],
    "note": "Initial schema: bench_runs + bench_verdicts with run_id FK"
}

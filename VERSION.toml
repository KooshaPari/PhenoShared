# Phenotype GFX SDK — umbrella version manifest
# Single source of truth for module versions in this monorepo.
sdk = "0.2.0"

[modules]
voxel   = "0.2.0"   # src/voxel — canonical in phenotype-gfx (was: <REDACTED>/phenotype-voxel, archived)
terrain = "0.2.0"   # unity/terrain — canonical in phenotype-gfx (was: <REDACTED>/phenotype-terrain, archived)
water   = "0.2.0"   # unity/water — canonical in phenotype-gfx (was: <REDACTED>/phenotype-water, archived)
postfx  = "0.2.0"   # unity/postfx — upstream: <REDACTED>/phenotype-postfx

# evdev-Class Input APIs, Routers, Virtual Devices, and Passthrough


> **Research date:** 2026-08-28  
> **Method:** capability-class comparison, not an assertion that every row is a drop-in competitor. Product status and exact feature matrices change; primary project/vendor links are recorded per row. Vendor performance claims are treated as claims until reproduced under the reference scenarios.  
> **Decision rule:** integration value is determined by measured stage cost, semantic fidelity, control/API quality, security boundary, and maintenance risk—not brand recognition.


## Scope and result

There is no literal evdev competitor: evdev is a Linux kernel ABI. This comparison maps **46 primitives and products** around acquisition, transformation, desktop mediation, guest delivery, USB forwarding and Windows/macOS equivalents.

| # | Primitive / project | Role | Natural path | Strength | Boundary / weakness | Fabric use | Primary source |
|---|---|---|---|---|---|---|---|
| 1 | evdev | Linux kernel generic input-event character-device ABI. | Physical/logical event acquisition; timestamps, event codes and device capabilities. | Canonical Linux source for keyboard, pointer, controller and sensor events. | Not a product, network protocol or policy engine. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://docs.kernel.org/input/input.html |
| 2 | libevdev | Userspace library wrapping evdev parsing, state and device handling. | Linux local API. | Safer implementation foundation than hand-parsing input_event streams. | Still requires permissions, grabs, routing and virtual targets. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://www.freedesktop.org/wiki/Software/libevdev/ |
| 3 | uinput | Linux kernel interface for userspace-created virtual input devices. | Local virtual HID-like endpoint. | Essential target mechanism for persistent host/VM route endpoints. | Consumer sees a virtual Linux device, not a cross-OS identity by itself. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://docs.kernel.org/input/uinput.html |
| 4 | hidraw | Raw HID report access. | Local raw device reports. | Needed for unusual devices and exact vendor reports. | Bypasses normalization and increases parsing/security burden. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://docs.kernel.org/hid/hidraw.html |
| 5 | HID-BPF | Programmable kernel-side HID report fixes and behavior. | Linux local kernel hook. | Good for quirks and selective transformations. | Not a distributed router; kernel safety and deployment matter. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://docs.kernel.org/hid/hid-bpf.html |
| 6 | libinput | Compositor/input-stack library for pointer, touchpad, tablet and touch semantics. | Local desktop semantic processing. | Best for acceleration, gestures and normalized desktop behavior. | Wrong layer for raw game/VM passthrough. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://wayland.freedesktop.org/libinput/doc/latest/ |
| 7 | macOS IOHIDManager / DriverKit HID | macOS device-level HID discovery and reports. | Local macOS HID. | Counterpart for endpoint-specific device integration. | Requires macOS entitlements/driver model and does not provide generic remote injection. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://developer.apple.com/documentation/iokit/iohidmanager_h |
| 8 | Windows Raw Input | Per-device keyboard/mouse/HID messages delivered to Windows applications. | Local Windows acquisition. | Preserves device distinction better than ordinary window messages. | Application-scoped and not itself a virtual device or system router. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://learn.microsoft.com/en-us/windows/win32/inputdev/raw-input |
| 9 | Windows HID APIs | SetupAPI/HIDClass-level access to devices and reports. | Local Windows raw/device control. | Specialist peripheral integration. | Driver/device complexity and permissions. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://learn.microsoft.com/en-us/windows-hardware/drivers/hid/ |
| 10 | SDL input | Portable application abstraction for keyboard, pointer and controllers. | Application-level cross-platform. | Useful endpoint adapter for games/test harnesses. | Cannot seize or route the whole OS seat. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://wiki.libsdl.org/ |
| 11 | QEMU input-linux | QEMU consumes Linux evdev devices directly with grab support. | Same-host host-to-guest. | Simple high-fidelity VM input baseline. | Individual QEMU ownership does not scale to arbitrary routing and can strand host control. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://www.qemu.org/docs/master/system/invocation.html |
| 12 | libvirt evdev input XML | Declarative configuration of host evdev devices for QEMU guests. | Same-host VM configuration. | Operationally convenient and reproducible. | Still binds devices to VM definitions rather than a central route broker. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://libvirt.org/formatdomain.html |
| 13 | evsieve | Event filter/split/remap pipeline that reads evdev and emits uinput. | Linux local routing primitive. | Closest ready-made building block for central host input composition. | No cross-device policy/control plane by itself. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/KarsMulder/evsieve |
| 14 | evdev-helper | Scripts that configure evdev/QEMU host↔guest switching with the both-Control-key convention. | Linux host to QEMU/libvirt guest. | Directly reflects the current VFIO workflow and exposes setup prior art. | Helper/script scope, direct device ownership and no distributed lease/state model. | Treat as migration/input for the central broker rather than a foundation. | https://github.com/pavolelsig/evdev_helper |
| 15 | libvirt-evdev / switching helpers | Scripts/daemons that rebind or switch evdev devices among guests. | Same-host VM. | Useful prior art for hotkey-driven VM focus. | Usually imperative and brittle; lacks fencing, pressed-state ledger and fleet semantics. | Integrate or wrap at its natural layer; do not flatten it into generic key events. |  |
| 16 | Interception Tools (Linux) | Low-level input interception plugin pipeline. | Linux local. | Composable remapping/transformation. | Not distributed and often keyboard-centric. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://gitlab.com/interception/linux/tools |
| 17 | keyd | System-wide Linux keyboard remapping daemon. | Linux local. | Fast keyboard policy/layers. | Does not route pointers/devices across realms. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/rvaiya/keyd |
| 18 | KMonad | Programmable cross-platform keyboard layers. | Local semantic remapping. | Rich portable keyboard behavior. | Not pointer/device/seat routing. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/kmonad/kmonad |
| 19 | Kanata | Cross-platform keyboard remapper with layers and chords. | Local semantic remapping. | Modern programmable keyboard policy. | Not network/seat/output fabric. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/jtroo/kanata |
| 20 | xremap | Application-aware key remapping for Linux desktops. | Linux desktop semantic layer. | Context-aware shortcut translation. | Compositor/platform constraints; not raw routing. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/xremap/xremap |
| 21 | input-remapper | GUI-oriented Linux input remapping. | Linux local. | Accessible configuration. | Not a deterministic distributed data plane. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/sezanzeb/input-remapper |
| 22 | evremap | Rust evdev-to-uinput remapper. | Linux local. | Good implementation reference for event transformation. | Narrow product scope. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/wez/evremap |
| 23 | keymapper | Cross-platform contextual key mapping. | Application/local semantic layer. | Portable shortcut abstraction. | Not device ownership or network routing. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/houmain/keymapper |
| 24 | InputPlumber | Composes physical controllers into virtual devices/profiles. | Linux local/session. | Strong prior art for capability-driven input graph composition. | Controller/gaming focus and not a whole distributed seat fabric. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/ShadowBlip/InputPlumber |
| 25 | libei / EIS | Wayland-era emulated input protocol between clients and compositor-mediated services. | Local IPC/authorized session. | Preferred modern injection path where compositor supports it. | Availability and permission behavior vary by compositor. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://libinput.pages.freedesktop.org/libei/ |
| 26 | XDG InputCapture portal | User-authorized input capture and release through desktop portal. | Wayland/compositor-mediated. | Security-correct cross-device capture model. | Consent/session lifecycle prevents invisible universal grabs. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.InputCapture.html |
| 27 | XDG RemoteDesktop portal | Authorized screen/input/clipboard remote-desktop session; EIS recommended for input. | Wayland/compositor-mediated. | Core Linux endpoint integration. | Capabilities vary and are explicitly permission-bound. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.RemoteDesktop.html |
| 28 | XTest | X11 synthetic input extension. | X11 local. | Broad legacy injection compatibility. | Coarse, insecure by modern standards and X11-only. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://www.x.org/releases/current/doc/xextproto/xtest.html |
| 29 | XInput2 | Richer X11 device/event model. | X11 local. | Per-device and advanced event semantics. | X11-only and not network routing. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://www.x.org/releases/current/doc/inputproto/XI2proto.txt |
| 30 | ydotool | Daemon/uinput-based synthetic input automation. | Linux local. | Practical automation fallback outside X11. | Privilege and security boundary; not source-device fidelity. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/ReimuNotMoe/ydotool |
| 31 | wtype | Wayland virtual keyboard/text injection utility. | Wayland local. | Narrow text/key automation. | Protocol/compositor support and no pointer/raw device path. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/atx/wtype |
| 32 | virtio-input | Paravirtualized input device for guests. | Same-host host-to-guest shared/virtio path. | Efficient stable guest endpoint. | Requires hypervisor/guest support and does not acquire physical devices. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://docs.oasis-open.org/virtio/virtio/v1.2/virtio-v1.2.html |
| 33 | QEMU usb-host | Passes an entire physical USB device to a guest. | Same-host USB passthrough. | High compatibility for devices that reject event translation. | Device disappears from host; dynamic sharing is coarse. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://www.qemu.org/docs/master/system/devices/usb.html |
| 34 | USB/IP | Exports USB transactions over IP. | LAN/WAN raw USB. | Exact specialist device semantics. | Latency, disconnects and security can break time-sensitive devices. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://docs.kernel.org/usb/usbip_protocol.html |
| 35 | usbredir / SPICE USB redirection | Session-oriented USB forwarding into VMs. | Host/client to guest. | Better UX than raw USB/IP in SPICE environments. | Session/protocol coupling and device-specific compatibility. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://www.spice-space.org/usbredir.html |
| 36 | VirtualHere | Commercial USB-over-IP. | LAN/WAN raw USB. | Polished specialist-device forwarding. | Commercial and still subject to physical USB latency/timing. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://www.virtualhere.com/ |
| 37 | VFIO USB-controller passthrough | Passes a whole PCIe USB controller to a VM. | Same-host PCIe/IOMMU. | Highest compatibility and isolation for many USB devices. | Cannot flexibly share ports; topology and IOMMU groups constrain design. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://www.kernel.org/doc/html/latest/driver-api/vfio.html |
| 38 | Windows SendInput | High-level system input injection API. | Windows local. | Simple first endpoint for keyboard/mouse. | Some software distinguishes synthetic input; no true per-device virtual HID. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-sendinput |
| 39 | Windows Interception driver | Low-level keyboard/mouse filter-driver ecosystem. | Windows kernel/user boundary. | More control/fidelity than SendInput. | Driver signing, security and maintenance burden. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/oblitum/Interception |
| 40 | Windows Virtual HID Framework | Kernel/driver framework for virtual HID source devices. | Windows local virtual endpoint. | Correct long-term true virtual keyboard/mouse/controller path. | Requires driver engineering, signing and narrow privileged code. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://learn.microsoft.com/en-us/windows-hardware/drivers/hid/virtual-hid-framework--vhf- |
| 41 | ViGEmBus | Virtual gamepad bus formerly used broadly on Windows. | Windows local virtual controller. | Important prior art and compatibility surface. | Archived/deprecated; not a new foundation. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/nefarius/ViGEmBus |
| 42 | macOS CGEvent / Event Taps | Observe/post desktop-level input events with Accessibility permission. | macOS local. | Practical initial endpoint implementation. | Secure Input, permissions and app protections constrain it. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://developer.apple.com/documentation/coregraphics/cgevent |
| 43 | Karabiner-Elements | Deep macOS keyboard remapping and virtual keyboard machinery. | macOS local. | Strong keyboard policy adapter/reference. | Not general remote pointer/device routing. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://karabiner-elements.pqrs.org/ |
| 44 | Steam Input | Per-game controller translation/remapping. | Application/game ecosystem. | Broad controller compatibility. | Steam/game-specific and not an OS fabric. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://partner.steamgames.com/doc/features/steam_controller |
| 45 | AntiMicroX | Maps controller input to keyboard/mouse actions. | Application/local. | Useful compatibility bridge. | Semantic emulation, not exact controller virtualization. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://github.com/AntiMicroX/antimicrox |
| 46 | OpenTabletDriver | Cross-platform graphics-tablet input stack. | Local device-specific. | Relevant for pen pressure/tablet routing. | Separate specialized semantics; requires dedicated port type and calibration. | Integrate or wrap at its natural layer; do not flatten it into generic key events. | https://opentabletdriver.net/ |

## Product conclusion

The baseline stack is evdev/libevdev for Linux physical acquisition, uinput for stable virtual endpoints, libei/EIS and portals for compositor-authorized Wayland injection, virtio/QEMU endpoints for guests, SendInput then VHF on Windows, and CGEvent then deeper HID integrations on macOS. Raw USB is an exception path for devices whose semantics cannot safely be translated.
